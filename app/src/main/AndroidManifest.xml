<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.apellikka.runmint"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".application.RunMintApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_runmint"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_runmint"
        android:supportsRtl="true"
        android:theme="@style/Theme.RunMint"
        tools:targetApi="31">
        <activity
            android:name=".activities.MainUiActivity"
            android:exported="true"
            android:theme="@style/Theme.RunMint">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>


</manifest>